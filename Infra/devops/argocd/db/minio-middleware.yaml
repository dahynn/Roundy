apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: minio-strip
  namespace: roundy-app
spec:
  stripPrefix:
    prefixes:
      - /minio-console
      - /minio-api
    forceSlash: true
